# Repositories to track, one "owner/name" per line.
# This is the single source of truth for the repo list (read by
# scripts/fetch_traffic.py). Blank lines and #comments are ignored.
itsab1989/ChromIQ
itsab1989/ChromIQ-Patches

# The dashboard itself (Knut, 2026-08-13). Its own 30-minute collector run
# clones this repo, so both clone columns - total and unique - mostly count
# GitHub's runners. With no releases and ~2 views a fortnight, this repo is
# the control case: it shows what the clone columns measure when nobody visits.
itsab1989/github-traffic-downloads-dashboard
