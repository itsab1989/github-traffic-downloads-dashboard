# Python dependencies for the traffic dashboard scripts.
#
# The default dashboard (interactive charts via dashboard.html + chart-data.json)
# is pure standard library and needs NOTHING here. matplotlib is only required for
# the optional legacy static-PNG mode (INCLUDE_GRAPHS=True in generate_dashboard.py);
# it is imported lazily, so the scripts and tests run fine without it installed.
matplotlib>=3.7
